version: '1'
bootstrap:
    boot_source_uri: file://${PUSHPY_START_HOME}/boot/boot.py
manager:
    auth_key: 'password'
sync_obj:
    port: 10000
    peers:
        - localhost:10001
        - localhost:10002
